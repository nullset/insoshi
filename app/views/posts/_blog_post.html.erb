<h2> 
  <a name="post_<%= blog_post.id %>"></a>
  <%= link_to sanitize(blog_post.title),
              person_blog_post_path(@blog.person, @blog, blog_post),
              :class => "show-follow" %>
  <%- if current_person?(@blog.person) -%>
    <%= link_to image_tag("icons/page_white_edit.png",
                          :class => 'actioner'),
                edit_person_blog_post_path(@blog.person, @blog, blog_post) %>
    <%= link_to image_tag("icons/page_white_delete.png",
                          :class => 'actioner'),
                person_blog_post_path(@blog.person, @blog, blog_post),
                :method => :delete,
                :confirm => "Delete this post?" %>
  <%- end -%>
</h2>
<%= display blog_post.body %>
<p class="meta">
  Posted <%= time_ago_in_words(blog_post.created_at) %> ago | 
  <%- n = blog_post.comments.all.count -%>
  <%= link_to pluralize(n, "comment"),
              person_blog_post_path(@blog.person, @blog, blog_post, :comments => n),
              :class => "show-follow" %>    
</p>
